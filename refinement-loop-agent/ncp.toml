[project]
name = "refinement-loop-agent"
version = "0.1.0"
description = "Change-plan drafting refined iteratively with LoopAgent until a reviewer approves it"
author = "NCP Team"

[build]
python_version = "3.11"
entry_point = "agents.main_agent:agent"

# Platform credentials (added by 'ncp authenticate')
# DO NOT commit this section to version control!
# [platform]
# url = "https://ncp.example.com"
# api_key = "your-api-key-here"
