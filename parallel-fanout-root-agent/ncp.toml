[project]
name = "parallel-fanout-root-agent"
version = "0.1.0"
description = "Multi-region health check deployed with its enclosing SequentialAgent as the entry point itself"
author = "NCP Team"

[build]
python_version = "3.11"
entry_point = "agents.main_agent:regional_health_check"

# Platform credentials (added by 'ncp authenticate')
# DO NOT commit this section to version control!
# [platform]
# url = "https://ncp.example.com"
# api_key = "your-api-key-here"
