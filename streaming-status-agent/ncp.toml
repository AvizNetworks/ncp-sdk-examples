[project]
name = "streaming-status-agent"
version = "0.1.0"
description = "Agent demonstrating send_status() and send_content() for long-running tool progress"
author = "NCP Team"

[build]
python_version = "3.11"
entry_point = "agents.main_agent:agent"

# Platform credentials (added by 'ncp authenticate')
# DO NOT commit this section to version control!
# [platform]
# url = "https://ncp.example.com"
# api_key = "your-api-key-here"
