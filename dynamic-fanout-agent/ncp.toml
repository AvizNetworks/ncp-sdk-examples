[project]
name = "dynamic-fanout-agent"
version = "0.1.0"
description = "Runtime-sized fan-out where a planner decides how many regions DynamicParallelAgent checks"
author = "NCP Team"

[build]
python_version = "3.11"
entry_point = "agents.main_agent:agent"

# Platform credentials (added by 'ncp authenticate')
# DO NOT commit this section to version control!
# [platform]
# url = "https://ncp.example.com"
# api_key = "your-api-key-here"
